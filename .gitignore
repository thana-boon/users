/node_modules
/.next
/out
/build
next-env.d.ts
/.claude

# env / secrets
.env
.env.local
.env.*.local

# never commit source data dumps with PII
/.example/*.xlsx
/.example/*.csv

# mediapipe wasm runtime — staged from node_modules by scripts/copy-mediapipe.mjs
# (~22MB of binaries; the .tflite model beside it IS committed)
/public/mediapipe/wasm

*.tsbuildinfo
npm-debug.log*
.DS_Store
env-users.txt